---
title: "Figma Components and Instances (beginner guide)"
description: "Build your first reusable component and understand the main-vs-instance relationship. — step-by-step on Mac."
pubDate: 2026-06-29
updatedDate: 2026-06-29
author: "Mohamed Saleh Zaied"
category: how-to
series: "Figma"
lessonNumber: 5
tags:
  - figma
  - ui-design
  - beginner
  - design
howTo:
  tools:
    - "Figma"
  steps:
    - name: "Select a frame and create a component with `Option Command K"
      text: "Select a frame and create a component with `Option Command K`"
    - name: "Recognize the purple diamond icon that marks a main componen"
      text: "Recognize the purple diamond icon that marks a main component in the Layers panel"
    - name: "Drag an instance out of the Assets panel onto the canvas"
      text: "Drag an instance out of the Assets panel onto the canvas"
    - name: "Edit the main component and watch every instance update"
      text: "Edit the main component and watch every instance update"
    - name: "Override a property on a single instance (change its text, f"
      text: "Override a property on a single instance (change its text, fill, etc.)"
    - name: "Reset overrides from the right sidebar"
      text: "Reset overrides from the right sidebar"
    - name: "Detach an instance with `Option Command B`"
      text: "Detach an instance with `Option Command B` — and understand this is one-way"
    - name: "Name components with slashes (e.g. `Button/Primary`) to orga"
      text: "Name components with slashes (e.g. `Button/Primary`) to organize them in the Assets panel"
faq:
  - question: "What's the fastest way to learn components and instances in Figma?"
    answer: "Work through the steps on this page in Figma itself, on your own project. For hands-free help, Skilly is a voice guide that watches your screen and moves your cursor to the exact button as you go — so you learn by doing instead of pausing a video. It's free to start at tryskilly.app."
  - question: "Do I need the paid version of Figma?"
    answer: "No — everything in this components and instances lesson works in the standard version of Figma on macOS. A few advanced features may require a paid tier, which we call out where relevant."
canonicalKeyword: "components and instances figma"
relatedArticles: []
---

Build your first reusable component and understand the main-vs-instance relationship. Here's how to do it in **Figma** on macOS, step by step — part of the free Figma beginner curriculum.

> **Lesson 5 of the Figma curriculum.** Before this: [Auto Layout](//). Next up: [Variants and Component Properties](//).

## What you'll do

- Select a frame and create a component with `Option Command K`
- Recognize the purple diamond icon that marks a main component in the Layers panel
- Drag an instance out of the Assets panel onto the canvas
- Edit the main component and watch every instance update
- Override a property on a single instance (change its text, fill, etc.)
- Reset overrides from the right sidebar
- Detach an instance with `Option Command B` — and understand this is one-way
- Name components with slashes (e.g. `Button/Primary`) to organize them in the Assets panel

## Do it hands-free with Skilly

Reading steps is one thing; doing them while the menus are in front of you is another. [Skilly](/) is a voice-first tutor for macOS: you ask out loud — "where's the components panel?" — and it answers while **moving your cursor to the exact button**, watching your actual Figma window. It's the same Figma curriculum these lessons come from, taught live instead of read. Free to start.

## Keep going

This is one stage of the full **[Figma beginner curriculum](//)**. Previous: [Auto Layout](//). Continue with [Variants and Component Properties](//).
