---
// src/components/Skills.astro
import { skills } from '../data/skills.ts';

const statusLabel: Record<string, string> = {
  available: 'AVAILABLE',
  coming_soon: 'COMING SOON',
  request: 'REQUEST A SKILL',
};
---
<section id="skills" class="relative py-24 lg:py-32 px-6">
  <div
    aria-hidden="true"
    class="absolute left-0 right-0 top-0 h-px"
    style="background: linear-gradient(90deg, transparent, #27272A, transparent);"
  ></div>

  <div class="max-w-site mx-auto">
    <div class="max-w-[640px] mx-auto text-center mb-16 lg:mb-20">
      <span class="text-[13px] font-semibold text-amber-500 tracking-[1.5px] uppercase">Skills</span>
      <h2 data-reveal class="reveal text-3xl sm:text-4xl lg:text-[44px] font-bold tracking-tight mt-3 mb-5 text-gray-50">
        One companion, many skills.
      </h2>
      <p data-reveal class="reveal reveal-delay-1 text-[17px] leading-relaxed text-gray-400">
        Skilly works with any app out of the box — just open it and start asking. Skills take it further: they teach Skilly each app's exact UI layout, keyboard shortcuts, and common pitfalls so it can point more accurately and guide you through structured lessons. Blender and Figma skills are live. More are coming.
      </p>
    </div>

    <div class="grid sm:grid-cols-2 lg:grid-cols-3 gap-5">
      {skills.map((skill, i) => {
        const isAvailable = skill.status === 'available';
        const isComingSoon = skill.status === 'coming_soon';
        const isRequest = skill.status === 'request';
        const delay = Math.min(i + 1, 5);

        const baseClasses = `reveal reveal-delay-${delay} relative rounded-2xl p-7 transition-all duration-300`;

        const stateClasses = isAvailable
          ? 'bg-gray-900 border border-amber-500/30 hover:border-amber-500/50 hover:-translate-y-1'
          : isComingSoon
            ? 'bg-gray-900/60 border border-gray-800 hover:border-gray-700'
            : 'bg-transparent border-2 border-dashed border-amber-500/35 hover:border-amber-500/60 hover:-translate-y-1';

        const wrapperClass = `${baseClasses} ${stateClasses}`;

        const pillClasses = isAvailable
          ? 'bg-green-500/10 border border-green-500/25 text-green-500'
          : isComingSoon
            ? 'bg-gray-800 border border-gray-700 text-gray-400'
            : 'bg-amber-500/15 border border-amber-500/25 text-amber-300';

        const Tag = isRequest ? 'a' : 'div';
        const tagProps = isRequest
          ? {
              href: skill.href,
              'data-ph-event': 'web_skill_request_clicked',
              class: `${wrapperClass} block group`,
            }
          : { class: wrapperClass };

        return (
          <Tag data-reveal {...tagProps}>
            <div class="flex items-center justify-between mb-5">
              <span class={`inline-flex items-center gap-1.5 text-[10px] font-mono font-semibold tracking-wider uppercase px-2.5 py-1 rounded ${pillClasses}`}>
                {isAvailable && (
                  <span class="block w-1.5 h-1.5 rounded-full bg-green-500 animate-pulse-soft" aria-hidden="true"></span>
                )}
                {statusLabel[skill.status]}
              </span>
              {isRequest && (
                <svg width="18" height="18" viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="text-amber-500 transition-transform duration-300 group-hover:translate-x-0.5 group-hover:-translate-y-0.5" aria-hidden="true">
                  <path d="M4 12L12 4M12 4H5M12 4V11"/>
                </svg>
              )}
            </div>

            <h3 class={`text-xl lg:text-2xl font-bold tracking-tight ${isComingSoon ? 'text-gray-300' : 'text-gray-50'}`}>
              {skill.name}
            </h3>

            {isAvailable && skill.description && (
              <p class="text-[14px] text-gray-400 mt-1.5 leading-relaxed">{skill.description}</p>
            )}

            {isRequest && (
              <p class="text-[14px] text-gray-400 mt-1.5 leading-relaxed">
                Tell us which app you'd learn with Skilly.
              </p>
            )}
          </Tag>
        );
      })}
    </div>

    <p class="text-center text-[14px] text-gray-500 mt-12 max-w-[560px] mx-auto leading-relaxed">
      Skills are built and maintained by Skilly. If you want to build your own,
      {' '}
      <a href="mailto:hello@tryskilly.app" class="text-amber-500 hover:text-amber-400 underline underline-offset-2">get in touch</a>
      {' '}
      — we're exploring a creator program.
    </p>

    <!-- Skill request form -->
    <div id="skill-request" class="max-w-[480px] mx-auto mt-16 scroll-mt-24">
      <div data-skill-request-wrapper class="bg-gray-900 rounded-[20px] border border-gray-800 p-8">
        <h3 class="text-lg font-semibold text-gray-50 mb-1">Request a skill</h3>
        <p class="text-sm text-gray-400 mb-6 leading-relaxed">Tell us which app you'd learn with Skilly. We read every request.</p>

        <form data-skill-request-form>
          <div class="flex flex-col gap-4 mb-4">
            <div>
              <label class="text-sm font-medium text-gray-300 block mb-1.5" for="sr-app">App name</label>
              <input
                id="sr-app"
                type="text"
                name="appName"
                placeholder="e.g. Unity, Houdini, Cinema 4D..."
                required
                class="w-full px-4 py-3 rounded-lg text-[15px] bg-gray-950 border border-gray-700 text-gray-200 placeholder-gray-600 outline-none focus:border-amber-500 transition-colors"
              />
            </div>
            <div>
              <label class="text-sm font-medium text-gray-300 block mb-1.5" for="sr-email">Your email</label>
              <input
                id="sr-email"
                type="email"
                name="email"
                placeholder="you@example.com"
                required
                class="w-full px-4 py-3 rounded-lg text-[15px] bg-gray-950 border border-gray-700 text-gray-200 placeholder-gray-600 outline-none focus:border-amber-500 transition-colors"
              />
            </div>
            <div>
              <label class="text-sm font-medium text-gray-300 block mb-1.5" for="sr-message">What would you learn? <span class="text-gray-500 font-normal">(optional)</span></label>
              <textarea
                id="sr-message"
                name="message"
                rows="2"
                placeholder="e.g. character rigging, 3D environments..."
                class="w-full px-4 py-3 rounded-lg text-[15px] bg-gray-950 border border-gray-700 text-gray-200 placeholder-gray-600 outline-none focus:border-amber-500 transition-colors resize-none"
              ></textarea>
            </div>
          </div>
          <button
            type="submit"
            disabled
            class="w-full bg-amber-500 hover:bg-amber-600 text-gray-950 rounded-lg px-6 py-3 text-[15px] font-semibold transition-all opacity-50 cursor-not-allowed"
          >
            Submit request
          </button>
          <p class="text-xs text-gray-600 mt-2.5 text-center">We'll email you if this skill becomes available.</p>
        </form>
      </div>

      <div data-skill-request-success hidden class="bg-gray-900 rounded-[20px] border border-amber-500/25 p-10 text-center">
        <div class="w-14 h-14 rounded-full bg-amber-500/10 border-2 border-amber-500/25 flex items-center justify-center mx-auto mb-5">
          <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="#F59E0B" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
            <path d="M5 12l5 5L20 7" />
          </svg>
        </div>
        <h3 class="text-[22px] font-semibold text-gray-50 mb-2">Request received!</h3>
        <p class="text-[15px] text-gray-400 leading-relaxed">
          We'll notify you when a <span data-success-app class="text-amber-300 font-medium"></span> skill is available.
        </p>
      </div>
    </div>
  </div>
</section>

<script>
  import '../scripts/skill-request.ts';
</script>
